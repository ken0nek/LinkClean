# The Deliverfile allows you to store various App Store Connect metadata
# For more information, check out the docs
# https://docs.fastlane.tools/actions/deliver/

# Listing localized to ja-JP + de-DE (2026-06-25); the binary has shipped ja/de
# strings since 1.1.0. release_notes ("What's New") is en-US only so far — the
# release-notes skill owns per-version localization; ja/de What's New is needed
# before the next versioned upload (or use --skip... for a metadata-only push).
# Screenshots are still en-US only (fastlane/screenshots/en-US/).
languages(["en-US", "ja", "de-DE"])
